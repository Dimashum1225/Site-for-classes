<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Панель управления</title>
    <style>
        body {
    font-family: 'Arial', sans-serif;
    margin: 0;
    padding: 0;
    background-color: #f0f0f0;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    background-image: url('/public/фон\ профиль.jpeg');
}

.dashboard {
    width: 80%;
    padding: 30px;
    background-color: #fff;
    border-radius: 10px;
    box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
    text-align: center;
}

.admin-dashboard {
    background-color: #f9f9f9;
}

.dashboard-heading {
    font-size: 2em;
    color: #333;
    margin-bottom: 20px;
}

.admin-links {
    display: flex;
    flex-direction: column;
    align-items: center;
}

.dashboard-link {
    display: inline-block;
    padding: 10px 20px;
    margin: 10px 0;
    background-color: #3498db;
    color: #fff;
    text-decoration: none;
    border-radius: 5px;
    transition: background-color 0.3s ease;
}

.dashboard-link:hover {
    background-color: #2980b9;
}

    </style>
</head>
<body>
    <% if (session.user && session.user.role === 'admin') { %>
    <div class="dashboard admin-dashboard">
        <h1 class="dashboard-heading">Добро пожаловать, Администратор</h1>
        <div class="admin-links">
            <a href="/dashboard/user-data" class="dashboard-link">Добавить данные пользователя</a>
            <a href="/dashboard/profile/<%= session.user._id %>" class="dashboard-link">Просмотреть профиль</a>
            <a href="/dashboard/add-news" class="dashboard-link">Добавить новость</a>
            <a href="/dashboard/edit-news" class="dashboard-link">Редактировать новости</a>
        </div>
    </div>
    <% } else if (session.user && session.user.role === 'user') { %>
    <!-- Ваш код для пользователя -->
    <% } else { %>
    <div class="dashboard">
        <h1 class="dashboard-heading">Пожалуйста, войдите</h1>
    </div>
    <% } %>
</body>
</html>
